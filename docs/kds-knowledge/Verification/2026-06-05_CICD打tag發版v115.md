---
type: verification
status: pass
created: 2026-06-05
tags:
  - type/verification
  - status/pass
valid_under: release.yml 觸發 v*;repo 含 keystore;UPLOAD_API_KEY 有效;hq.citrus.tw fileUploader 可用
---
# CI/CD 打 tag 發版 端到端驗證 (v1.1.5)

## 結果：PASS

## 測試項目
1. **本機 `assembleRelease`** → BUILD SUCCESSFUL，APK 簽章 `DN: O=Citrus`（確認 repo 內建 keystore 可簽，CI 免 secret）。✓
2. **bump 1.1.4→1.1.5(code16) + 打 tag `v1.1.5` + push**。✓
3. **GitHub Actions run 全綠**(4m14s,所有步驟 ✓:Parse tag→assembleRelease→產 version.json→上傳 hq.citrus.tw→artifact)。✓
4. **伺服器實測**：
   - `https://hq.citrus.tw/apk/kds/citruskds_v1.1.5.apk` → HTTP 200, 37.2MB, `application/vnd.android.package-archive`。✓
   - `https://hq.citrus.tw/apk/kds/version.json` → latestVersion 1.1.5 / code 16 / md5 / downloadUrl 正確。✓

## 相關模組
- [[CICD]]
- [[版本更新系統]]

## 備註
- `UPLOAD_API_KEY` 已設於 repo secret。
- Node.js 20 actions deprecation 警告(非阻擋,2026-06-16 強制 Node 24)。
