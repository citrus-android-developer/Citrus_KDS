---
type: verification
status: pass
date: 2026-06-30
valid_under: 後端 GetOrdersList 以 AppendAddQty 把第一語言 AddGName 結尾 *N 補進 Addition2
revalidate_when: 加料數量編碼格式改變 / Addition2 來源(Goods.GName2)改變
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:實機 PASS(2026-06-30),英文顯示模式加料帶數量(卡片+列印),後端 DLL 部署後
  VERIFY:本機 API 驗 milk more*1/onion more*6 + 實機測試 OK
related:
  - "[[Systems/POS-API端點]]"
---
# 2026-06-30_加料第二語言數量

英文(及英文為主)顯示模式下，加料數量(`*N`)在卡片與收據顯示。

## 問題
加料數量 `*N` 只存在第一語言 `AddGName`(如 `多牛奶*1`);第二語言 `Addition2`=`Goods.GName2` 為純品名(`milk more`)→ 英文模式 `additionDisplay` 取第二語言時掉數量(卡片+列印都中,因兩者共用 `additionDisplay`)。47 號單(W012026063000047)測試回報。

## 修法(後端單獨改,前端/列印不動)
- `KitchenDisplayDAL.AppendAddQty(AddGName, AddGName2)`:取第一語言結尾 `*數字` 補到 Addition2。
- 單一加料適用;複合加料 AddGName2 本為 null → 前端 fallback 第一語言(已含數量),不受影響。
- commit `Compass_KDS` f6d5883。

## 驗證
- 本機跑新 DLL 打 API(47 單):`Addition2` = `milk more*1` / `milk little*7` / `noddles more*1` / `onion more*6` ✅
- **實機測試 OK**(2026-06-30,後端 DLL 部署後):英文模式加料帶數量 ✅ PASS
- 列印無需改:`EscPos` 走同一 `additionDisplay`,Addition2 帶數量後收據自動帶數量

## 關聯
- [[Systems/POS-API端點]]
