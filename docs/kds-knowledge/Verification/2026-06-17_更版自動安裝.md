---
type: verification
status: pass
feature: 版本更新下載完自動跳系統安裝器 + 對話框自動關閉（補完 NO_AUTO_INSTALL）
commit: 工作區（尚未 commit）
date: 2026-06-17
valid_under:
  - "FileProvider authority = kds.provider（AndroidManifest）"
  - "下載存檔 = getExternalFilesDir(DOWNLOADS)/compass_kds.apk（KtorDownloadUseCase.getFile 與 MainActivity 一致）"
  - "minSdk 24；canRequestPackageInstalls 僅 API26+ 檢查"
  - "REQUEST_INSTALL_PACKAGES 權限已宣告"
revalidate_when:
  - "下載存檔路徑或 APK_FILE_NAME 改變"
  - "FileProvider authority / provider_paths 改變"
  - "targetSdk 升級導致安裝 intent/權限行為變動"
tags:
  - type/verification
  - status/pass
related:
  - "[[Systems/版本更新系統]]"
---
# 更版下載完自動安裝 驗證

## 已驗（code 層）
- `compileDebugKotlin` BUILD SUCCESSFUL（imports/型別/Composable scope 皆過）。
- 實機 SM-X710 `install -r` 後 `am start` → app 正常啟動（pid 存活、logcat 無 FATAL）→ `registerForActivityResult` 欄位初始化與 `LaunchedEffect` effect collector 不造成啟動崩潰。

## ⚠️ 尚未驗（裝置端 end-to-end）
- **下載完成後「安裝器是否真的跳出 + 對話框是否關閉」尚未目視確認**（未驅動 UI 跑完整下載流程）。
- 此機裝 debug、下載的 `citruskds_v1.1.7.apk` 是 CICD release 簽章 → 即使安裝器跳出，也會因**簽章不符**拒裝。要完整驗「安裝完成」需簽章一致的 production 機。
- → 本 Verification 僅背書「code 實作正確、編譯、無啟動崩潰」；UI 端到端與安裝完成待後續（production 機或驅動 UI）。

## 變更
- `MainActivity.kt`：effect collector（DownloadApkSuccess → dismiss + installApk）、`installApk()`/`launchApkInstaller()`、`unknownSourcesLauncher`。

## 相關
- [[Systems/版本更新系統]]
