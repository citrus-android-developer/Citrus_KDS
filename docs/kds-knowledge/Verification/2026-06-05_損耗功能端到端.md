---
type: verification
status: pass
created: 2026-06-05
tags:
  - type/verification
  - status/pass
valid_under: 後端已部署至 192.168.0.162 (order DB);product.dbo.Goods 提供 GName/Desc/Price;GPrice 採單價×Qty
---
# 損耗(Wastage)功能端到端驗證

## 結果：PASS

透過本地 POS API（與 App 完全相同的端點與 ApiKey）送出損耗,並查 DB 確認寫入正確。

## 測試項目
1. **InventoryList 撈品項** → 取 GID=1001 / GKID=110(小菜1234),SellStatus=Available。✓
2. **POST /KDS/SetWastage**(Status=S 損耗, Qty=2, CreateUser=KDS_TEST)→ `ApiStatus:1` / HTTP 200。✓
3. **DB `order.dbo.Wastage` 寫入驗證**(itemseq=82):
   - GKID=110 / GID=1001 / Qty=2 / Status=S / CreateUser=KDS_TEST（前端帶入）✓
   - Gname=小菜1234 / sizedesc=.（取自 product.dbo.Goods）✓
   - **GPrice=20.0 = Price(10) × Qty(2)** ✓
   - Flag=A / CreateDate=GETDATE()（後端填）✓
4. **清理**：測試列已 DELETE,Wastage 此品項回到 0 筆。✓

## 相關模組
- [[SetStockPage]]（損耗 UI：stepper + 送出按鈕）
- [[POS-API端點]]（/KDS/SetWastage）

## 備註
- 後端 DAL：`INSERT INTO Wastage ... SELECT FROM product.dbo.Goods WHERE GID AND GKID AND Flag!='D'`。
- App UI 僅送 **損耗(S)**；報廢(W)後端支援但前端無入口。
- 邊界：GID/GKID 不存在時 SELECT 0 列、仍回 ApiStatus:1（不會報錯,但也不寫入）—— 未在此次測試涵蓋。
