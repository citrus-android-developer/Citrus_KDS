---
type: verification
status: pass
date: 2026-06-26
valid_under: 後端回傳 OrdersData.KDSName(PrintGroup.Name 對 KDS_ID)
revalidate_when: 收據開頭版面改動 / KDS_ID↔PrintGroup 對應規則變更
tags:
  - type/verification
  - status/pass
summary: |-
  TEST:實機列印驗證 PASS(2026-06-26),收據開頭印群組名 Citrus(非純 ID 01)
  VERIFY:後端 b975be3 + 前端 1.1.16(code27) 兩端上線後實機印單確認
related:
  - "[[列印系統]]"
---
# 2026-06-26_收據開頭群組名

收據開頭由純 KDS_ID(如 `01`)改印 KDS 群組名(`PrintGroup.Name`,如 `Citrus`),空則 fallback `prefs.kdsId`。

## 改動
- **後端** `Compass_KDS` b975be3：`GetOrdersList` 跨庫查 `product.dbo.PrintGroup WHERE ID=@KDS_ID` 取 `Name` → 每張 `OrdersData.KDSName`(獨立 scalar 查,不動主查詢/GROUP BY)。
- **前端** 1.1.16(versionCode 27)：`Order.kdsName`(@Json `KDSName`);`EscPosReceiptBuilder` 開頭印 `order.kdsName?.ifBlank{null} ?: prefs.kdsId`。

## 驗證
- 本機後端：`KDS_ID=01` → `KDSName=Citrus` ✅
- **實機列印**(兩端上線後)：收據開頭印 `Citrus`,非 `01` ✅ PASS
- fallback：舊後端/未設名稱 → kdsName 空 → 印 KDS_ID(不回歸)

## 關聯
- [[列印系統]]
