---
type: system
status: done
created: 2026-05-13
updated: 2026-06-05
tags:
  - type/system
  - status/done
summary: |-
  FLOW:fetchOrdersJob(3秒,動作後1秒) / fetchOrderReadyJob(3秒)｜兩者互斥
  KEY:fetchOrders只抓currentPage,切Tab後下次輪詢才同步,animateBufferGap控制加速,main成功後觸發自動接單派發
  DEP:[[POS-API端點]][[KDS訂單管理]][[OrderReady模式]][[自動接單功能]]
---
# 輪詢架構

App 透過兩個互斥的輪詢 Job 持續從 POS 拉取資料：

| Job | 啟動者 | 抓什麼 | 間隔 |
|-----|--------|--------|------|
| `orderInfoJob` | `startFetchKdsInfo` | 依 `currentPage` 抓 main/served/recall | 3 秒（動作後 1 秒） |
| `orderReadyJob` | `startFetchOrderReadyInfo` | `/controller/OrdersList` | 3 秒 |

兩者**互斥**：啟動其中一個必先 `stop` 另一個（`CentralViewModel.kt:503-523`）。

## fetchOrdersJob 詳細

`CentralViewModel.kt:772-784`：

```kotlin
private fun fetchOrdersJob(): Flow<Job> = flow {
    while (currentCoroutineContext().isActive) {
        if (!animateBufferGap) {
            fetchOrders()
            delay(3000)
        } else {
            delay(1000)
            animateBufferGap = false
            fetchOrders()
        }
    }
}
```

- 正常情況：每 3 秒輪一次
- 設了 `animateBufferGap=true`（執行完 SetOrderStatus 後會設）：等 1 秒再 fetch，配合 UI 卡片淡出動畫

## fetchOrders 只抓當前 Tab

`CentralViewModel.kt:822-866`：

```kotlin
repository.getOrders(type = currentState.currentPage.lowercase())
```

Response 依 `currentPage` 寫入不同 state 欄位（main/served/recall）。
切 Tab 時 `updateCurrentPage(index)` 改 `currentPage`，下次輪詢就拉新 list。

**副作用**：背景 Tab 的 list 不會即時刷新，需要靠：
- 操作當下對「非當前 Tab」list 做 `isVisible=false` 客戶端隱藏
- 切過去時的下一次輪詢真正同步

## fetchOrderReadyJob

`CentralViewModel.kt:786-791`：純粹固定 3 秒輪詢，沒有 animateBufferGap 機制（OrderReady 模式不會有 UI 操作）。

## 啟動點

| 進入畫面 | LaunchedEffect 觸發 | 對應 Job |
|----------|---------------------|----------|
| KdsScreen | `startFetchKdsInfo` | `orderInfoJob` |
| OrderReadyScreen | `startFetchOrderReadyInfo` | `orderReadyJob` |

## 關聯

- 使用此機制的頁面：[[KDS訂單管理]] / [[OrderReady模式]]
- 涉及的 API：[[POS-API端點]]
- 動作後加速補位的搭配：[[訂單狀態流轉]]


## 2026-06-03 更新：自動接單派發掛點

`fetchOrders` main 成功分支在 `setState` 之後呼叫 `autoAcceptNewOrders(result.data)`，把 J(NEW) 單自動 `setOrderStatus(W)`（需 autoAccept + prepareMode 皆開）。等於每次輪詢都是自動接單的觸發點（最多 3 秒延遲）。詳見 [[自動接單功能]]。



## 2026-06-04 連線設定未完成前不輪詢
- **問題**：還沒填連線參數就一直輪詢 → API 失敗 → 錯誤訊息一直彈。
- **修法**：`fetchOrders()` 開頭 `if (!shouldPollOrders()) return@launch`。
- `shouldPollOrders()` = `currentPage in orderPages && prefs.localIp 非空 && (prefs.kdsId 非空 || prefs.mode != 0)`。
  - 即：訂單頁 + 已填本地 IP + (KDS 模式要有 kdsId / 非 KDS 模式免)。
- 輪詢迴圈(`fetchOrdersJob` while + delay 3s)照跑，但條件不符時該次直接跳過不發 request → 設定完成後下一輪自然開始輪詢。
- 來源 commit 7446076。